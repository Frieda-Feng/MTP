# 任务一 heat.py 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development 逐任务实现。步骤用 `- [ ]` 复选框跟踪。

**Goal:** 实现 `mission1/heat.py`，用 Jacobi / Gauss-Seidel 迭代法解铁板稳态热传导 Ax=b，输出 6 张静态 PNG + 3 张独立 GIF。

**Architecture:** 单文件函数化（低技术债）。流程：建模 `build_*` → 求解 `jacobi / gauss_seidel / solve_direct` → 画图 `plot_* / make_*_gif` → `main()` 串联。测试用 `numpy.testing` 裸 assert（**不引入 pytest**，少依赖），`python mission1/test_heat.py` 运行。

**Tech Stack:** Python + numpy + matplotlib + Pillow（GIF 写出）

## Global Constraints（每个任务隐含遵守）

- **全中文**：代码注释、图标题/轴标签、README 均中文；与用户沟通也中文。
- **matplotlib 全局**：中文字体、`DPI=300`、统一色标 0–100°C（红=热蓝=冷，如 `RdBu_r`）。字体设置（macOS）：
  ```python
  import matplotlib.pyplot as plt
  plt.rcParams['font.sans-serif'] = ['Arial Unicode MS', 'PingFang SC', 'Heiti TC', 'DejaVu Sans']
  plt.rcParams['axes.unicode_minus'] = False
  plt.rcParams['figure.dpi'] = 300
  ```
- **数值参数**：`tol=1e-6`、`max_iter=10000`、`x0=zeros`、铁棍 `n=5`、铁板 `n=2`(验证)+`n=30`(主实验)、`T_top=100`、`T_others=0`。
- **可复现**：`pip install -r requirements.txt && python mission1/heat.py` 一键生成全部图与 GIF。
- **输出目录**：所有图存 `mission1/output/`。
- **上游设计**：`docs/任务一设计.md`（数学叙事）、`docs/superpowers/specs/2026-07-05-任务一实现-design.md`（本计划的上游 spec）。函数签名以 spec §3 为准。

---

### Task 1: 项目骨架 + 建模 + 直接解

**Files:**
- Create: `requirements.txt`
- Create: `mission1/heat.py`
- Create: `mission1/test_heat.py`
- Create: `mission1/output/.gitkeep`

**Interfaces (Produces):**
- `build_1d(n, T_left, T_right) -> (A: ndarray[n,n], b: ndarray[n])`
- `build_2d(n, T_top, T_others) -> (A: ndarray[n²,n²], b: ndarray[n²])`，节点编号 `idx(i,j) = i*n + j`（i=行，0=最上）
- `solve_direct(A, b) -> x`：`np.linalg.solve`

- [ ] **Step 1: 写 `requirements.txt`**
```
numpy
matplotlib
Pillow
```

- [ ] **Step 2: 写失败测试 `mission1/test_heat.py`**（覆盖 2×2 手算真值、1D 三对角与解析解）
```python
import numpy as np
from numpy.testing import assert_allclose
from heat import build_1d, build_2d, solve_direct

def test_build_2d_2x2():
    A, b = build_2d(2, 100, 0)
    A_exp = np.array([[4,-1,-1,0],[-1,4,0,-1],[-1,0,4,-1],[0,-1,-1,4]], dtype=float)
    assert_allclose(A, A_exp)
    assert_allclose(b, [100,100,0,0])

def test_solve_2x2():
    A, b = build_2d(2, 100, 0)
    assert_allclose(solve_direct(A, b), [37.5,37.5,12.5,12.5], atol=1e-9)

def test_build_1d_and_solve():
    A, b = build_1d(5, 100, 0)
    assert A.shape == (5,5) and b.shape == (5,)
    for i in range(5):
        for j in range(5):
            if abs(i-j) > 1: assert A[i,j] == 0  # 三对角
    x = solve_direct(A, b)
    assert_allclose(x, [100*(1-k/6) for k in range(1,6)], atol=1e-9)

def _run_all():
    for name, fn in sorted(globals().items()):
        if name.startswith("test_"): fn(); print(f"  ✓ {name}")

if __name__ == "__main__":
    _run_all(); print("Task1 OK")
```

- [ ] **Step 3: 运行确认失败**
Run: `cd mission1 && python test_heat.py`
Expected: `ModuleNotFoundError: No module named 'heat'`

- [ ] **Step 4: 实现 `mission1/heat.py` 的建模与直接解**
```python
"""任务一：迭代法解稳态热传导 Ax=b。单文件函数化。"""
import numpy as np
import matplotlib.pyplot as plt

plt.rcParams['font.sans-serif'] = ['Arial Unicode MS', 'PingFang SC', 'Heiti TC', 'DejaVu Sans']
plt.rcParams['axes.unicode_minus'] = False
plt.rcParams['figure.dpi'] = 300


def build_1d(n, T_left, T_right):
    """铁棍：n 个内部点，两端温度固定。三对角 A，2 在主对角、-1 在紧邻。"""
    A = np.zeros((n, n))
    b = np.zeros(n)
    for i in range(n):
        A[i, i] = 2
        if i > 0: A[i, i-1] = -1
        if i < n-1: A[i, i+1] = -1
    b[0] += T_left
    b[-1] += T_right
    return A, b


def build_2d(n, T_top, T_others):
    """铁板 n×n 内部点，五点差分。上边=T_top，左/右/下=T_others。
    方程：4·T_ij - T_上 - T_下 - T_左 - T_右 = b_ij；边界邻居移到 b。"""
    N = n * n
    A = np.zeros((N, N))
    b = np.zeros(N)
    def idx(i, j): return i * n + j
    for i in range(n):
        for j in range(n):
            k = idx(i, j)
            A[k, k] = 4
            # 上邻
            if i > 0: A[k, idx(i-1, j)] = -1
            else: b[k] += T_top
            # 下邻
            if i < n-1: A[k, idx(i+1, j)] = -1
            else: b[k] += T_others
            # 左邻
            if j > 0: A[k, idx(i, j-1)] = -1
            else: b[k] += T_others
            # 右邻
            if j < n-1: A[k, idx(i, j+1)] = -1
            else: b[k] += T_others
    return A, b


def solve_direct(A, b):
    """直接法（裁判）。"""
    return np.linalg.solve(A, b)
```

- [ ] **Step 5: 运行确认通过**
Run: `cd mission1 && python test_heat.py`
Expected: 三条 `✓` + `Task1 OK`

- [ ] **Step 6: Commit**
```bash
git add requirements.txt mission1/
git commit -m "feat(mission1): 建模 build_1d/build_2d + 直接解

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: Jacobi + Gauss-Seidel 迭代（含 snapshot_every）

**Files:**
- Modify: `mission1/heat.py`
- Modify: `mission1/test_heat.py`

**Interfaces:**
- Consumes: `build_2d`, `solve_direct`
- Produces: `jacobi(A, b, x0, tol=1e-6, max_iter=10000, snapshot_every=None) -> (x, err_hist, x_snaps)`；`gauss_seidel(...)` 同签名。`err_hist` 每轮 `‖x新−x旧‖∞`；`x_snaps` 按 `snapshot_every` 采样的 x 副本（含初值），`None` 则空表。

- [ ] **Step 1: 在 `test_heat.py` 追加失败测试**
```python
from heat import jacobi, gauss_seidel

def test_jacobi_2x2():
    A, b = build_2d(2, 100, 0)
    x, err, snaps = jacobi(A, b, np.zeros(4))
    assert_allclose(x, [37.5,37.5,12.5,12.5], atol=1e-6)
    assert len(snaps) == 0 and len(err) > 0

def test_iter_converge_30_and_gs_faster():
    A, b = build_2d(30, 100, 0)
    xd = solve_direct(A, b)
    xj, ej, sj = jacobi(A, b, np.zeros(900), snapshot_every=50)
    xg, eg, sg = gauss_seidel(A, b, np.zeros(900), snapshot_every=50)
    assert_allclose(xj, xd, atol=1e-4)
    assert_allclose(xg, xd, atol=1e-4)
    assert len(eg) < len(ej)          # GS 收敛更快
    assert len(sj) > 0 and len(sg) > 0
```
（同时把这两行加进 `_run_all` 自动收集，无需手动改——`startswith("test_")` 已覆盖。）

- [ ] **Step 2: 运行确认失败**
Run: `cd mission1 && python test_heat.py`
Expected: `ImportError: cannot import name 'jacobi'`

- [ ] **Step 3: 实现两个迭代函数**（追加到 `heat.py`）
```python
def jacobi(A, b, x0, tol=1e-6, max_iter=10000, snapshot_every=None):
    """Jacobi：整轮用旧值。"""
    n = len(b)
    x = np.asarray(x0, dtype=float).copy()
    x_new = x.copy()
    err_hist, snaps = [], []
    if snapshot_every: snaps.append(x.copy())
    for it in range(max_iter):
        for i in range(n):
            s = b[i] - A[i] @ x + A[i, i] * x[i]   # 减去 A[i,i]*x[i] 因含在对角
            x_new[i] = s / A[i, i]
        diff = np.max(np.abs(x_new - x))
        x, x_new = x_new, x        # 换引用
        err_hist.append(diff)
        if snapshot_every and (it + 1) % snapshot_every == 0:
            snaps.append(x.copy())
        if diff < tol:
            break
    return x, err_hist, snaps


def gauss_seidel(A, b, x0, tol=1e-6, max_iter=10000, snapshot_every=None):
    """Gauss-Seidel：算过的分量立即用新值（in-place）。"""
    n = len(b)
    x = np.asarray(x0, dtype=float).copy()
    err_hist, snaps = [], []
    if snapshot_every: snaps.append(x.copy())
    for it in range(max_iter):
        x_old = x.copy()
        for i in range(n):
            s = b[i] - A[i] @ x + A[i, i] * x[i]
            x[i] = s / A[i, i]
        diff = np.max(np.abs(x - x_old))
        err_hist.append(diff)
        if snapshot_every and (it + 1) % snapshot_every == 0:
            snaps.append(x.copy())
        if diff < tol:
            break
    return x, err_hist, snaps
```

- [ ] **Step 4: 运行确认通过**
Run: `cd mission1 && python test_heat.py`
Expected: 五条 `✓`（含两条新测）。注意 30×30 的 Jacobi 可能上千轮、数秒，耐心等。

- [ ] **Step 5: Commit**
```bash
git add mission1/heat.py mission1/test_heat.py
git commit -m "feat(mission1): Jacobi + Gauss-Seidel 迭代（含采样）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 谱半径辅助 + 6 张静态图

**Files:**
- Modify: `mission1/heat.py`
- Modify: `mission1/test_heat.py`

**Interfaces:**
- Produces: `spectral_radius_jacobi(n) -> float`（cos(π/(n+1))）、`spectral_radius_gs(n)`（ρ_J²）、`plot_rod / plot_matrix_and_bars / plot_heatmap_compare / plot_error_curve / plot_method_bars / plot_divergence`（均 `(数据..., path)`，存 PNG 到 `output/`）。

- [ ] **Step 1: 追加谱半径测试**
```python
from heat import spectral_radius_jacobi, spectral_radius_gs
def test_spectral_radius():
    assert_allclose(spectral_radius_jacobi(30), np.cos(np.pi/31), atol=1e-12)
    assert_allclose(spectral_radius_gs(30), spectral_radius_jacobi(30)**2, atol=1e-12)
```

- [ ] **Step 2: 运行确认失败**（`ImportError: cannot import name 'spectral_radius_jacobi'`）

- [ ] **Step 3: 实现谱半径**（追加 `heat.py`）
```python
def spectral_radius_jacobi(n):
    return np.cos(np.pi / (n + 1))

def spectral_radius_gs(n):
    return spectral_radius_jacobi(n) ** 2
```

- [ ] **Step 4: 实现 6 个画图函数**（追加 `heat.py`）。每个函数：建 figure → 画 → `fig.savefig(path, dpi=300, bbox_inches='tight')` → `plt.close(fig)`。色标统一 `vmin=0, vmax=100, cmap='RdBu_r'`。具体每张：
  - `plot_rod(x_5, path)`：折线，X=位置1–5，Y=温度；同时画解析直线 `100*(1-k/6)`；图例「直接解」「解析解」；标题「铁棍稳态温度：直接解 vs 解析解」。
  - `plot_matrix_and_bars(A_4x4, x_4, path)`：左子图 `plt.spy(A)`（标题「4×4 矩阵稀疏结构」），右子图柱状图 4 点温度；总标题。
  - `plot_heatmap_compare(x_iter, x_direct, n, path)`：左右两个 `imshow`（reshape 成 (n,n)，`cmap='RdBu_r', vmin=0, vmax=100`），各加 `colorbar`；标题分别「迭代解」「直接解」。
  - `plot_error_curve(err_j, err_g, path)`：`plt.semilogy` 两条（Jacobi / GS），X=迭代轮数，Y=误差，网格，图例，标题「迭代误差曲线（半对数）」。
  - `plot_method_bars(stats, path)`：`stats` = dict 如 `{'Jacobi':(轮数,误差,耗时),'Gauss-Seidel':(...),'直接法':(...)}`；三个子图分组条形（轮数 / 误差 / 耗时 ms）；标题「三方法对比」。
  - `plot_divergence(err_div, path)`：`semilogy` 单条发散曲线；标题「不收敛反例：A=[[1,2],[2,1]]」。

- [ ] **Step 5: 手动冒烟测试**（在 `mission1/` 起 `python -c` 跑一遍生成 6 张图，确认无异常且文件非空）
Run:
```bash
cd mission1 && python -c "
import numpy as np, os
from heat import *
os.makedirs('output', exist_ok=True)
A2,b2 = build_2d(2,100,0); x2 = solve_direct(A2,b2)
A1,b1 = build_1d(5,100,0); x1 = solve_direct(A1,b1)
A,b = build_2d(30,100,0); xd = solve_direct(A,b)
xj,ej,_ = jacobi(A,b,np.zeros(900)); xg,eg,_ = gauss_seidel(A,b,np.zeros(900))
plot_rod(x1,'output/fig1_rod.png')
plot_matrix_and_bars(A2,x2,'output/fig2_matrix.png')
plot_heatmap_compare(xg,xd,30,'output/fig3_heatmap.png')
plot_error_curve(ej,eg,'output/fig4_error.png')
plot_method_bars({'Jacobi':(len(ej),ej[-1],50),'Gauss-Seidel':(len(eg),eg[-1],30),'直接法':(0,0,200)},'output/fig5_bars.png')
A_div = np.array([[1,2],[2,1]],dtype=float)
_,ed,_ = jacobi(A_div,np.array([1.,1.]),max_iter=30)
plot_divergence(ed,'output/fig6_diverge.png')
print('6 figures saved')
"
```
Expected: `6 figures saved`，`output/` 下 6 个 PNG 且每个 > 10KB。

- [ ] **Step 6: Commit**
```bash
git add mission1/heat.py mission1/test_heat.py mission1/output/
git commit -m "feat(mission1): 谱半径 + 6 张静态图

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: 3 张独立 GIF

**Files:**
- Modify: `mission1/heat.py`

**Interfaces:**
- Produces: `make_iter_gif(x_snaps, n, T_top, title, path, fps=8)`、`make_direct_gif(x_final, n, T_top, title, path)`。帧 = 把扁平 x reshape (n,n) 后嵌入含边界 (n+2,n+2)（顶行=T_top，其余三边=T_others=0），`imshow(cmap='RdBu_r', vmin=0, vmax=100)`。

- [ ] **Step 1: 实现 GIF 函数**（追加 `heat.py`）
```python
from matplotlib.animation import FuncAnimation, PillowWriter

def _embed_boundary(x_flat, n, T_top, T_others=0.0):
    """把 n×n 内部解嵌入 (n+2)×(n+2) 含边界温度场，便于画完整铁板。"""
    interior = x_flat.reshape(n, n)
    full = np.full((n+2, n+2), T_others, dtype=float)
    full[1:-1, 1:-1] = interior
    full[0, :] = T_top            # 顶边
    return full

def make_iter_gif(x_snaps, n, T_top, title, path, fps=8):
    frames = [_embed_boundary(s, n, T_top) for s in x_snaps]
    fig, ax = plt.subplots(figsize=(5, 4.5))
    im = ax.imshow(frames[0], cmap='RdBu_r', vmin=0, vmax=100, origin='upper')
    ax.set_title(title); fig.colorbar(im, ax=ax, label='温度 °C')
    def update(k):
        im.set_data(frames[min(k, len(frames)-1)])
        ax.set_xlabel(f'第 {min(k,len(frames)-1)} 帧')
        return [im]
    anim = FuncAnimation(fig, update, frames=len(frames), interval=1000//fps, blit=False)
    anim.save(path, writer=PillowWriter(fps=fps))
    plt.close(fig)

def make_direct_gif(x_final, n, T_top, title, path, fps=2):
    """两帧：初值全 0 → 最终解。"""
    zero = np.zeros(n*n)
    make_iter_gif([zero, x_final], n, T_top, title, path, fps=fps)
```

- [ ] **Step 2: 冒烟测试生成 3 张 GIF**
Run:
```bash
cd mission1 && python -c "
import numpy as np
from heat import *
A,b = build_2d(30,100,0)
xd = solve_direct(A,b)
_,_,sj = jacobi(A,b,np.zeros(900),snapshot_every=30)
_,_,sg = gauss_seidel(A,b,np.zeros(900),snapshot_every=20)
make_iter_gif(sj,30,100,'Jacobi 迭代','output/jacobi.gif')
make_iter_gif(sg,30,100,'Gauss-Seidel 迭代','output/gauss_seidel.gif')
make_direct_gif(xd,30,100,'直接法（一步求解）','output/direct.gif')
print('3 gifs saved')
"
```
Expected: `3 gifs saved`，`output/` 下 3 个 `.gif` 文件且每个 > 50KB。

- [ ] **Step 3: Commit**
```bash
git add mission1/heat.py mission1/output/
git commit -m "feat(mission1): 3 张独立 GIF（direct/jacobi/gauss_seidel）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: main() 串联 + README + 端到端运行

**Files:**
- Modify: `mission1/heat.py`（加 `main()` 与 `if __name__=='__main__'`）
- Create: `mission1/README.md`

- [ ] **Step 1: 实现 `main()`**——按 spec §8 串联：建摸(1D+2D 2×2+2D 30×30)→三方法求解(记录耗时)→6 静态图→3 GIF→打印收敛轮数/谱半径/误差报告。耗时用 `time.perf_counter()`。`plot_method_bars` 的 stats 用真实测得的轮数/误差/耗时。

- [ ] **Step 2: 写 `mission1/README.md`**（中文）：项目简介、依赖（`pip install -r requirements.txt`）、运行命令（`python mission1/heat.py`）、输出清单（6 PNG + 3 GIF，每张说明展示什么）、可复现说明、AI 工具使用声明（Claude Code 协助生成代码）。

- [ ] **Step 3: 端到端运行**
Run: `cd mission1 && python heat.py`
Expected: 无异常；终端打印收敛轮数/谱半径/耗时；`output/` 下 9 个文件齐全（6 PNG + 3 GIF）。打开几张图肉眼检查：图3 热力图顶部红、三边蓝；图4 GS 曲线在 Jacobi 之下；jacobi.gif 能看到热量从顶部下渗。

- [ ] **Step 4: Commit**
```bash
git add mission1/heat.py mission1/README.md
git commit -m "feat(mission1): main 串联 + README，端到端可复现

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 完成标准（Definition of Done）

- [ ] `python mission1/test_heat.py` 全绿
- [ ] `python mission1/heat.py` 生成 6 PNG + 3 GIF，肉眼检查符合物理直觉
- [ ] Jacobi/GS 收敛到直接解（误差 < 1e-4）；GS 迭代轮数约为 Jacobi 一半
- [ ] 谱半径打印值与 `cos(π/31)`、`cos²(π/31)` 吻合
- [ ] README 含运行说明 + AI 使用声明
